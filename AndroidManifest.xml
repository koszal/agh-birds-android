<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.ogorzalek.birds"
    android:versionCode="1"
    android:versionName="1.0" >
    
	<uses-sdk
		android:minSdkVersion="8"
		android:targetSdkVersion="15" />
	
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" 
        android:theme="@android:style/Theme.Holo.NoActionBar">
        <activity
            android:name=".activities.StartActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AboutActivity"></activity>
        <activity android:name=".activities.AdvancedSearchActivity"></activity>
        <activity android:name=".activities.BirdActivity"></activity>
        <activity android:name=".activities.BirdListActivity"></activity>
        <activity android:name=".activities.QuizActivity"></activity>
        <activity android:name=".activities.QuizLauncherActivity"></activity>
        <activity android:name=".activities.SettingsActivity"></activity>
    </application>

</manifest>